def main():
    
    n = int(input("Ingrese la cantidad de números a evaluar: "))

    count_within_range = 0

    for i in range(n):
        num = float(input(f"Ingrese el número {i + 1}: "))
        if 10 <= num <= 20:
            count_within_range += 1

    print(f"De los {n} números ingresados, {count_within_range} están en el rango de 10 - 20.")

if __name__ == "__main__":
    main()